const defaultConfig = require('tailwindcss/defaultConfig');
module.exports      = {
  theme: {
    extend: {
      fontFamily: {
        'sketch': ['Cabin Sketch'],
        'neucha': ['Neucha'],
      },
    },
  },
};
